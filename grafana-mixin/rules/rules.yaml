groups:
  - name: grafana_rules
    rules:
    # Record error rate of http requests excluding dataproxy, /ds/query and /tsdb/query requests
    - record: job_handler_statuscode:http_request_total:rate5m
      expr: |
          sum by (job, handler, statuscode) (rate(http_request_total[5m]))
