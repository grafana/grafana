package v2beta1

// Dashboard Context contains structured metadata for a dashboard with properties that do not belong to main spec
Context: {
  // Default layout that would be used when adding new containers (rows, tabs)
  defaultLayoutType?: "GridLayout" | "AutoGridLayout"
}
