package v0alpha1

// AlertRuleQualityPolicySingletonName is the only allowed name for an
// AlertRuleQualityPolicy resource. The policy is a per-org singleton, so it
// always lives at this fixed name.
const AlertRuleQualityPolicySingletonName = "default"
