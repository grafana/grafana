package v0alpha1

// ConfigSingletonName is the only allowed name for a Config resource. Config is
// a per-org singleton, so it always lives at this fixed name.
const ConfigSingletonName = "default"
