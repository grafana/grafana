apiVersion: apps.grafana.app/v1alpha2
kind: AppManifest
metadata:
  name: alerting
spec:
  appDisplayName: alerting
  appName: alerting
  group: rules.alerting.grafana.app
  preferredVersion: v0alpha1
  versions:
  - kinds:
    - admission:
        mutation:
          operations:
          - CREATE
          - UPDATE
        validation:
          operations:
          - CREATE
          - UPDATE
          - DELETE
      conversion: false
      kind: AlertRule
      plural: AlertRules
      schemas:
        AlertRule:
          properties:
            spec:
              $ref: '#/components/schemas/spec'
            status:
              $ref: '#/components/schemas/status'
          required:
          - spec
        DatasourceUID:
          pattern: ^[a-zA-Z0-9_-]+$
          type: string
        ExecErrState:
          enum:
          - Error
          - Ok
          - Alerting
          - KeepLast
          type: string
        Expression:
          additionalProperties: false
          properties:
            datasourceUID:
              $ref: '#/components/schemas/DatasourceUID'
              description: The UID of the datasource to run this expression against.
                If omitted, the expression will be run against the `__expr__` datasource
            model:
              additionalProperties: {}
              type: object
            queryType:
              description: The type of query if this is a query expression
              type: string
            relativeTimeRange:
              $ref: '#/components/schemas/RelativeTimeRange'
            source:
              description: |-
                Used to mark the expression to be used as the final source for the rule evaluation
                Only one expression in a rule can be marked as the source
                For AlertRules, this is the expression that will be evaluated against the alerting condition
                For RecordingRules, this is the expression that will be recorded
              type: boolean
          required:
          - model
          type: object
        ExpressionMap:
          additionalProperties:
            $ref: '#/components/schemas/Expression'
          type: object
        IntervalTrigger:
          additionalProperties: false
          properties:
            interval:
              $ref: '#/components/schemas/PromDuration'
          required:
          - interval
          type: object
        NamedRoutingTree:
          additionalProperties: false
          properties:
            routingTree:
              type: string
            type:
              $ref: '#/components/schemas/NotificationSettingsType'
          required:
          - type
          - routingTree
          type: object
        NoDataState:
          enum:
          - NoData
          - Ok
          - Alerting
          - KeepLast
          type: string
        NotificationSettings:
          oneOf:
          - $ref: '#/components/schemas/SimplifiedRouting'
          - $ref: '#/components/schemas/NamedRoutingTree'
        NotificationSettingsType:
          enum:
          - SimplifiedRouting
          - NamedRoutingTree
          type: string
        OperatorState:
          additionalProperties: false
          properties:
            descriptiveState:
              description: descriptiveState is an optional more descriptive state
                field which has no requirements on format
              type: string
            details:
              additionalProperties: true
              description: details contains any extra information that is operator-specific
              type: object
            lastEvaluation:
              description: lastEvaluation is the ResourceVersion last evaluated
              type: string
            state:
              description: |-
                state describes the state of the lastEvaluation.
                It is limited to three possible states for machine evaluation.
              enum:
              - success
              - in_progress
              - failed
              type: string
          required:
          - lastEvaluation
          - state
          type: object
        PanelRef:
          additionalProperties: false
          properties:
            dashboardUID:
              minLength: 1
              pattern: ^[a-zA-Z0-9_-]+$
              type: string
            panelID:
              exclusiveMinimum: true
              minimum: 0
              type: integer
          required:
          - dashboardUID
          - panelID
          type: object
        PromDuration:
          not:
            pattern: hmuµn
          pattern: ^((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?|0)$
          type: string
        PromDurationWMillis:
          pattern: ^((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?|0)$
          type: string
        RelativeTimeRange:
          additionalProperties: false
          properties:
            from:
              $ref: '#/components/schemas/PromDurationWMillis'
            to:
              $ref: '#/components/schemas/PromDurationWMillis'
          required:
          - from
          - to
          type: object
        SimplifiedRouting:
          additionalProperties: false
          properties:
            activeTimeIntervals:
              items:
                $ref: '#/components/schemas/TimeIntervalRef'
              type: array
            groupBy:
              items:
                type: string
              type: array
            groupInterval:
              $ref: '#/components/schemas/PromDuration'
            groupWait:
              $ref: '#/components/schemas/PromDuration'
            muteTimeIntervals:
              items:
                $ref: '#/components/schemas/TimeIntervalRef'
              type: array
            receiver:
              type: string
            repeatInterval:
              $ref: '#/components/schemas/PromDuration'
            type:
              $ref: '#/components/schemas/NotificationSettingsType'
          required:
          - type
          - receiver
          type: object
        TemplateString:
          type: string
        TimeIntervalRef:
          type: string
        spec:
          additionalProperties: false
          properties:
            annotations:
              additionalProperties:
                $ref: '#/components/schemas/TemplateString'
              type: object
            execErrState:
              $ref: '#/components/schemas/ExecErrState'
              default: Error
            expressions:
              $ref: '#/components/schemas/ExpressionMap'
            for:
              type: string
            keepFiringFor:
              type: string
            labels:
              additionalProperties:
                $ref: '#/components/schemas/TemplateString'
              type: object
            missingSeriesEvalsToResolve:
              minimum: 0
              type: integer
            noDataState:
              $ref: '#/components/schemas/NoDataState'
              default: NoData
            notificationSettings:
              $ref: '#/components/schemas/NotificationSettings'
            panelRef:
              $ref: '#/components/schemas/PanelRef'
            paused:
              type: boolean
            title:
              type: string
            trigger:
              $ref: '#/components/schemas/IntervalTrigger'
          required:
          - title
          - trigger
          - noDataState
          - execErrState
          - expressions
          type: object
        status:
          additionalProperties: false
          properties:
            additionalFields:
              additionalProperties: true
              description: additionalFields is reserved for future use
              type: object
            operatorStates:
              additionalProperties:
                $ref: '#/components/schemas/OperatorState'
              description: |-
                operatorStates is a map of operator ID to operator state evaluations.
                Any operator which consumes this kind SHOULD add its state evaluation information to this field.
              type: object
          type: object
      scope: Namespaced
      selectableFields:
      - spec.title
      - spec.paused
      - spec.panelRef.dashboardUID
      - spec.panelRef.panelID
      - spec.notificationSettings.type
      - spec.notificationSettings.receiver
      - spec.notificationSettings.routingTree
      userReadable: false
    - admission:
        mutation:
          operations:
          - CREATE
          - UPDATE
        validation:
          operations:
          - CREATE
          - UPDATE
          - DELETE
      conversion: false
      kind: RecordingRule
      plural: RecordingRules
      schemas:
        DatasourceUID:
          pattern: ^[a-zA-Z0-9_-]+$
          type: string
        Expression:
          additionalProperties: false
          properties:
            datasourceUID:
              $ref: '#/components/schemas/DatasourceUID'
              description: The UID of the datasource to run this expression against.
                If omitted, the expression will be run against the `__expr__` datasource
            model:
              additionalProperties: {}
              type: object
            queryType:
              description: The type of query if this is a query expression
              type: string
            relativeTimeRange:
              $ref: '#/components/schemas/RelativeTimeRange'
            source:
              description: |-
                Used to mark the expression to be used as the final source for the rule evaluation
                Only one expression in a rule can be marked as the source
                For AlertRules, this is the expression that will be evaluated against the alerting condition
                For RecordingRules, this is the expression that will be recorded
              type: boolean
          required:
          - model
          type: object
        ExpressionMap:
          additionalProperties:
            $ref: '#/components/schemas/Expression'
          type: object
        IntervalTrigger:
          additionalProperties: false
          properties:
            interval:
              $ref: '#/components/schemas/PromDuration'
          required:
          - interval
          type: object
        MetricName:
          pattern: ^[a-zA-Z_:][a-zA-Z0-9_:]*$
          type: string
        OperatorState:
          additionalProperties: false
          properties:
            descriptiveState:
              description: descriptiveState is an optional more descriptive state
                field which has no requirements on format
              type: string
            details:
              additionalProperties: true
              description: details contains any extra information that is operator-specific
              type: object
            lastEvaluation:
              description: lastEvaluation is the ResourceVersion last evaluated
              type: string
            state:
              description: |-
                state describes the state of the lastEvaluation.
                It is limited to three possible states for machine evaluation.
              enum:
              - success
              - in_progress
              - failed
              type: string
          required:
          - lastEvaluation
          - state
          type: object
        PromDuration:
          not:
            pattern: hmuµn
          pattern: ^((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?|0)$
          type: string
        PromDurationWMillis:
          pattern: ^((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?(([0-9]+)ms)?|0)$
          type: string
        RecordingRule:
          properties:
            spec:
              $ref: '#/components/schemas/spec'
            status:
              $ref: '#/components/schemas/status'
          required:
          - spec
        RelativeTimeRange:
          additionalProperties: false
          properties:
            from:
              $ref: '#/components/schemas/PromDurationWMillis'
            to:
              $ref: '#/components/schemas/PromDurationWMillis'
          required:
          - from
          - to
          type: object
        TemplateString:
          type: string
        spec:
          additionalProperties: false
          properties:
            expressions:
              $ref: '#/components/schemas/ExpressionMap'
            labels:
              additionalProperties:
                $ref: '#/components/schemas/TemplateString'
              type: object
            metric:
              $ref: '#/components/schemas/MetricName'
            paused:
              type: boolean
            targetDatasourceUID:
              $ref: '#/components/schemas/DatasourceUID'
            title:
              type: string
            trigger:
              $ref: '#/components/schemas/IntervalTrigger'
          required:
          - title
          - trigger
          - metric
          - expressions
          - targetDatasourceUID
          type: object
        status:
          additionalProperties: false
          properties:
            additionalFields:
              additionalProperties: true
              description: additionalFields is reserved for future use
              type: object
            operatorStates:
              additionalProperties:
                $ref: '#/components/schemas/OperatorState'
              description: |-
                operatorStates is a map of operator ID to operator state evaluations.
                Any operator which consumes this kind SHOULD add its state evaluation information to this field.
              type: object
          type: object
      scope: Namespaced
      selectableFields:
      - spec.title
      - spec.paused
      - spec.metric
      - spec.targetDatasourceUID
      userReadable: false
    - admission:
        mutation:
          operations:
          - CREATE
          - UPDATE
        validation:
          operations:
          - CREATE
          - UPDATE
      conversion: false
      kind: RuleSequence
      plural: RuleSequences
      schemas:
        IntervalTrigger:
          additionalProperties: false
          properties:
            interval:
              $ref: '#/components/schemas/PromDuration'
          required:
          - interval
          type: object
        OperatorState:
          additionalProperties: false
          properties:
            descriptiveState:
              description: descriptiveState is an optional more descriptive state
                field which has no requirements on format
              type: string
            details:
              additionalProperties: true
              description: details contains any extra information that is operator-specific
              type: object
            lastEvaluation:
              description: lastEvaluation is the ResourceVersion last evaluated
              type: string
            state:
              description: |-
                state describes the state of the lastEvaluation.
                It is limited to three possible states for machine evaluation.
              enum:
              - success
              - in_progress
              - failed
              type: string
          required:
          - lastEvaluation
          - state
          type: object
        PromDuration:
          not:
            pattern: hmuµn
          pattern: ^((([0-9]+)y)?(([0-9]+)w)?(([0-9]+)d)?(([0-9]+)h)?(([0-9]+)m)?(([0-9]+)s)?|0)$
          type: string
        RuleRef:
          additionalProperties: false
          properties:
            name:
              $ref: '#/components/schemas/RuleUID'
              description: name is the metadata.name of an AlertRule or RecordingRule
                resource.
          required:
          - name
          type: object
        RuleSequence:
          properties:
            spec:
              $ref: '#/components/schemas/spec'
            status:
              $ref: '#/components/schemas/status'
          required:
          - spec
        RuleUID:
          pattern: ^[a-zA-Z0-9_-]+$
          type: string
        spec:
          additionalProperties: false
          properties:
            alertingRules:
              items:
                $ref: '#/components/schemas/RuleRef'
              type: array
            recordingRules:
              items:
                $ref: '#/components/schemas/RuleRef'
              type: array
            trigger:
              $ref: '#/components/schemas/IntervalTrigger'
          required:
          - trigger
          - recordingRules
          type: object
        status:
          additionalProperties: false
          properties:
            additionalFields:
              additionalProperties: true
              description: additionalFields is reserved for future use
              type: object
            operatorStates:
              additionalProperties:
                $ref: '#/components/schemas/OperatorState'
              description: |-
                operatorStates is a map of operator ID to operator state evaluations.
                Any operator which consumes this kind SHOULD add its state evaluation information to this field.
              type: object
          type: object
      scope: Namespaced
      userReadable: false
    name: v0alpha1
    routes:
      namespaced:
        /search:
          get:
            operationId: getSearchRules
            parameters:
            - in: query
              name: continueToken
              schema:
                type: string
            - in: query
              name: dashboardUID
              schema:
                type: string
            - in: query
              name: datasourceUIDs
              schema:
                items:
                  type: string
                type: array
            - description: |-
                Fields to count distinct terms for (e.g. "folder"). Returned under
                "facets" keyed by field name. Facet terms are ordered by count, not
                alphabetically.
              in: query
              name: facet
              schema:
                description: |-
                  Fields to count distinct terms for (e.g. "folder"). Returned under
                  "facets" keyed by field name. Facet terms are ordered by count, not
                  alphabetically.
                items:
                  type: string
                type: array
            - description: Max terms returned per facet (default 50, max 1000).
              in: query
              name: facetLimit
              schema:
                description: Max terms returned per facet (default 50, max 1000).
                type: integer
            - in: query
              name: folders
              schema:
                items:
                  type: string
                type: array
            - in: query
              name: groups
              schema:
                items:
                  type: string
                type: array
            - in: query
              name: labels
              schema:
                items:
                  type: string
                type: array
            - in: query
              name: limit
              schema:
                type: integer
            - in: query
              name: metric
              schema:
                type: string
            - in: query
              name: names
              schema:
                items:
                  type: string
                type: array
            - in: query
              name: notificationType
              schema:
                type: string
            - in: query
              name: panelID
              schema:
                type: integer
            - in: query
              name: paused
              schema:
                type: boolean
            - in: query
              name: q
              schema:
                type: string
            - in: query
              name: receiver
              schema:
                type: string
            - in: query
              name: routingTree
              schema:
                type: string
            - in: query
              name: sort
              schema:
                $ref: '#/components/schemas/RuleSearchSortField'
            - in: query
              name: targetDatasourceUID
              schema:
                type: string
            - in: query
              name: type
              schema:
                type: string
            responses:
              default:
                content:
                  application/json:
                    schema:
                      additionalProperties: false
                      properties:
                        apiVersion:
                          description: 'APIVersion defines the versioned schema of
                            this representation of an object. Servers should convert
                            recognized schemas to the latest internal value, and may
                            reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
                          type: string
                        facets:
                          additionalProperties:
                            $ref: '#/components/schemas/getSearchRulesFacetResult'
                          type: object
                        items:
                          items:
                            $ref: '#/components/schemas/getSearchRulesRuleHit'
                          type: array
                        kind:
                          description: 'Kind is a string value representing the REST
                            resource this object represents. Servers may infer this
                            from the endpoint the client submits requests to. Cannot
                            be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                          type: string
                        metadata:
                          properties:
                            continue:
                              type: string
                            remainingItemCount:
                              type: integer
                            resourceVersion:
                              type: string
                            selfLink:
                              type: string
                          type: object
                          x-grafana-app-uses-kubernetes-list-metadata: true
                      required:
                      - items
                      - apiVersion
                      - kind
                      - metadata
                      type: object
                description: Default OK response
        /search/alertrules:
          get:
            operationId: getSearchAlertRules
            parameters:
            - in: query
              name: continueToken
              schema:
                type: string
            - in: query
              name: dashboardUID
              schema:
                type: string
            - in: query
              name: datasourceUIDs
              schema:
                items:
                  type: string
                type: array
            - description: |-
                Fields to count distinct terms for (e.g. "folder"). Returned under
                "facets" keyed by field name. Facet terms are ordered by count, not
                alphabetically.
              in: query
              name: facet
              schema:
                description: |-
                  Fields to count distinct terms for (e.g. "folder"). Returned under
                  "facets" keyed by field name. Facet terms are ordered by count, not
                  alphabetically.
                items:
                  type: string
                type: array
            - description: Max terms returned per facet (default 50, max 1000).
              in: query
              name: facetLimit
              schema:
                description: Max terms returned per facet (default 50, max 1000).
                type: integer
            - in: query
              name: folders
              schema:
                items:
                  type: string
                type: array
            - in: query
              name: groups
              schema:
                items:
                  type: string
                type: array
            - in: query
              name: labels
              schema:
                items:
                  type: string
                type: array
            - in: query
              name: limit
              schema:
                type: integer
            - in: query
              name: names
              schema:
                items:
                  type: string
                type: array
            - in: query
              name: notificationType
              schema:
                type: string
            - in: query
              name: panelID
              schema:
                type: integer
            - in: query
              name: paused
              schema:
                type: boolean
            - in: query
              name: q
              schema:
                type: string
            - in: query
              name: receiver
              schema:
                type: string
            - in: query
              name: routingTree
              schema:
                type: string
            - in: query
              name: sort
              schema:
                $ref: '#/components/schemas/RuleSearchSortField'
            responses:
              default:
                content:
                  application/json:
                    schema:
                      additionalProperties: false
                      properties:
                        apiVersion:
                          description: 'APIVersion defines the versioned schema of
                            this representation of an object. Servers should convert
                            recognized schemas to the latest internal value, and may
                            reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
                          type: string
                        facets:
                          additionalProperties:
                            $ref: '#/components/schemas/getSearchAlertRulesFacetResult'
                          type: object
                        items:
                          items:
                            $ref: '#/components/schemas/getSearchAlertRulesAlertRuleHit'
                          type: array
                        kind:
                          description: 'Kind is a string value representing the REST
                            resource this object represents. Servers may infer this
                            from the endpoint the client submits requests to. Cannot
                            be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                          type: string
                        metadata:
                          properties:
                            continue:
                              type: string
                            remainingItemCount:
                              type: integer
                            resourceVersion:
                              type: string
                            selfLink:
                              type: string
                          type: object
                          x-grafana-app-uses-kubernetes-list-metadata: true
                      required:
                      - items
                      - apiVersion
                      - kind
                      - metadata
                      type: object
                description: Default OK response
        /search/recordingrules:
          get:
            operationId: getSearchRecordingRules
            parameters:
            - in: query
              name: continueToken
              schema:
                type: string
            - in: query
              name: datasourceUIDs
              schema:
                items:
                  type: string
                type: array
            - description: |-
                Fields to count distinct terms for (e.g. "folder"). Returned under
                "facets" keyed by field name. Facet terms are ordered by count, not
                alphabetically.
              in: query
              name: facet
              schema:
                description: |-
                  Fields to count distinct terms for (e.g. "folder"). Returned under
                  "facets" keyed by field name. Facet terms are ordered by count, not
                  alphabetically.
                items:
                  type: string
                type: array
            - description: Max terms returned per facet (default 50, max 1000).
              in: query
              name: facetLimit
              schema:
                description: Max terms returned per facet (default 50, max 1000).
                type: integer
            - in: query
              name: folders
              schema:
                items:
                  type: string
                type: array
            - in: query
              name: groups
              schema:
                items:
                  type: string
                type: array
            - in: query
              name: labels
              schema:
                items:
                  type: string
                type: array
            - in: query
              name: limit
              schema:
                type: integer
            - in: query
              name: metric
              schema:
                type: string
            - in: query
              name: names
              schema:
                items:
                  type: string
                type: array
            - in: query
              name: paused
              schema:
                type: boolean
            - in: query
              name: q
              schema:
                type: string
            - in: query
              name: sort
              schema:
                $ref: '#/components/schemas/RuleSearchSortField'
            - in: query
              name: targetDatasourceUID
              schema:
                type: string
            responses:
              default:
                content:
                  application/json:
                    schema:
                      additionalProperties: false
                      properties:
                        apiVersion:
                          description: 'APIVersion defines the versioned schema of
                            this representation of an object. Servers should convert
                            recognized schemas to the latest internal value, and may
                            reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
                          type: string
                        facets:
                          additionalProperties:
                            $ref: '#/components/schemas/getSearchRecordingRulesFacetResult'
                          type: object
                        items:
                          items:
                            $ref: '#/components/schemas/getSearchRecordingRulesRecordingRuleHit'
                          type: array
                        kind:
                          description: 'Kind is a string value representing the REST
                            resource this object represents. Servers may infer this
                            from the endpoint the client submits requests to. Cannot
                            be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                          type: string
                        metadata:
                          properties:
                            continue:
                              type: string
                            remainingItemCount:
                              type: integer
                            resourceVersion:
                              type: string
                            selfLink:
                              type: string
                          type: object
                          x-grafana-app-uses-kubernetes-list-metadata: true
                      required:
                      - items
                      - apiVersion
                      - kind
                      - metadata
                      type: object
                description: Default OK response
      schemas:
        getSearchAlertRulesAlertRuleHit:
          additionalProperties: false
          properties:
            annotations:
              additionalProperties:
                type: string
              type: object
            dashboardUID:
              type: string
            datasourceUIDs:
              items:
                type: string
              type: array
            folder:
              type: string
            for:
              type: string
            group:
              type: string
            interval:
              type: string
            keepFiringFor:
              type: string
            labels:
              additionalProperties:
                type: string
              type: object
            name:
              type: string
            notificationType:
              type: string
            panelID:
              type: integer
            paused:
              type: boolean
            receiver:
              type: string
            routingTree:
              type: string
            title:
              type: string
            type:
              $ref: '#/components/schemas/getSearchAlertRulesRuleSearchType'
          required:
          - type
          - name
          - title
          - folder
          type: object
        getSearchAlertRulesFacetResult:
          additionalProperties: false
          description: |-
            FacetResult is the distinct-term breakdown for one faceted field, e.g. the
            per-folder rule counts returned for facet=folder.
          properties:
            field:
              type: string
            missing:
              type: integer
            terms:
              items:
                $ref: '#/components/schemas/getSearchAlertRulesTermFacet'
              type: array
            total:
              type: integer
          required:
          - field
          - total
          - missing
          type: object
        getSearchAlertRulesRuleSearchType:
          enum:
          - alertrule
          - recordingrule
          type: string
        getSearchAlertRulesTermFacet:
          additionalProperties: false
          description: TermFacet is a single faceted term and the number of matching
            rules.
          properties:
            count:
              type: integer
            term:
              type: string
          required:
          - term
          - count
          type: object
        getSearchRecordingRulesFacetResult:
          additionalProperties: false
          description: |-
            FacetResult is the distinct-term breakdown for one faceted field, e.g. the
            per-folder rule counts returned for facet=folder.
          properties:
            field:
              type: string
            missing:
              type: integer
            terms:
              items:
                $ref: '#/components/schemas/getSearchRecordingRulesTermFacet'
              type: array
            total:
              type: integer
          required:
          - field
          - total
          - missing
          type: object
        getSearchRecordingRulesRecordingRuleHit:
          additionalProperties: false
          properties:
            datasourceUIDs:
              items:
                type: string
              type: array
            folder:
              type: string
            group:
              type: string
            interval:
              type: string
            labels:
              additionalProperties:
                type: string
              type: object
            metric:
              type: string
            name:
              type: string
            paused:
              type: boolean
            targetDatasourceUID:
              type: string
            title:
              type: string
            type:
              $ref: '#/components/schemas/getSearchRecordingRulesRuleSearchType'
          required:
          - type
          - name
          - title
          - folder
          type: object
        getSearchRecordingRulesRuleSearchType:
          enum:
          - alertrule
          - recordingrule
          type: string
        getSearchRecordingRulesTermFacet:
          additionalProperties: false
          description: TermFacet is a single faceted term and the number of matching
            rules.
          properties:
            count:
              type: integer
            term:
              type: string
          required:
          - term
          - count
          type: object
        getSearchRulesAlertRuleHit:
          additionalProperties: false
          properties:
            annotations:
              additionalProperties:
                type: string
              type: object
            dashboardUID:
              type: string
            datasourceUIDs:
              items:
                type: string
              type: array
            folder:
              type: string
            for:
              type: string
            group:
              type: string
            interval:
              type: string
            keepFiringFor:
              type: string
            labels:
              additionalProperties:
                type: string
              type: object
            name:
              type: string
            notificationType:
              type: string
            panelID:
              type: integer
            paused:
              type: boolean
            receiver:
              type: string
            routingTree:
              type: string
            title:
              type: string
            type:
              $ref: '#/components/schemas/getSearchRulesRuleSearchType'
          required:
          - type
          - name
          - title
          - folder
          type: object
        getSearchRulesFacetResult:
          additionalProperties: false
          description: |-
            FacetResult is the distinct-term breakdown for one faceted field, e.g. the
            per-folder rule counts returned for facet=folder.
          properties:
            field:
              type: string
            missing:
              type: integer
            terms:
              items:
                $ref: '#/components/schemas/getSearchRulesTermFacet'
              type: array
            total:
              type: integer
          required:
          - field
          - total
          - missing
          type: object
        getSearchRulesRecordingRuleHit:
          additionalProperties: false
          properties:
            datasourceUIDs:
              items:
                type: string
              type: array
            folder:
              type: string
            group:
              type: string
            interval:
              type: string
            labels:
              additionalProperties:
                type: string
              type: object
            metric:
              type: string
            name:
              type: string
            paused:
              type: boolean
            targetDatasourceUID:
              type: string
            title:
              type: string
            type:
              $ref: '#/components/schemas/getSearchRulesRuleSearchType'
          required:
          - type
          - name
          - title
          - folder
          type: object
        getSearchRulesRuleHit:
          description: RuleHit is the cross-kind union returned by /search.
          oneOf:
          - $ref: '#/components/schemas/AlertRuleHit'
          - $ref: '#/components/schemas/RecordingRuleHit'
        getSearchRulesRuleSearchType:
          enum:
          - alertrule
          - recordingrule
          type: string
        getSearchRulesTermFacet:
          additionalProperties: false
          description: TermFacet is a single faceted term and the number of matching
            rules.
          properties:
            count:
              type: integer
            term:
              type: string
          required:
          - term
          - count
          type: object
    served: true
