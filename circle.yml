machine:
  services:
    - docker

dependencies:
  override:
    - go get github.com/tools/godep
    - npm install

test:
  override:
    - godep go test -v ./pkg/...
    - ./node_modules/grunt-cli/bin/grunt test
