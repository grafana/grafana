FROM ubuntu:24.04

RUN --mount=type=cache,target=/var/lib/apt/lists \
  --mount=type=cache,target=/var/cache/apt \
  set -eux; \
  apt-get update; \
  apt-get install -y --no-install-recommends ca-certificates; \
  update-ca-certificates

WORKDIR /grafana

RUN mkdir -p "conf/provisioning/datasources" \
  "conf/provisioning/dashboards" \
  "conf/provisioning/notifiers" \
  "conf/provisioning/plugins" \
  "conf/provisioning/access-control" \
  "conf/provisioning/alerting"

COPY conf/defaults.ini conf/defaults.ini

COPY public/emails public/emails
COPY public/views public/views
COPY public/dashboards public/dashboards
COPY public/app/plugins public/app/plugins

# TODO: Remove below as part of https://github.com/grafana/grafana/issues/110350
COPY public/gazetteer public/gazetteer
COPY public/maps public/maps
COPY public/img/bg public/img/bg
COPY public/img/icons public/img/icons

ADD devenv/frontend-service/build/grafana bin/grafana

# The rspack build writes to public/build/rspack. Tilt passes the directory the
# backend will read from, so the container only carries one build's assets.
ARG BUILD_DIR=build
COPY public/${BUILD_DIR}/assets-manifest.json public/${BUILD_DIR}/assets-manifest.json
COPY public/${BUILD_DIR}/boot.js public/${BUILD_DIR}/boot.js

# fetched by @grafana/plugin-e2e at test runtime, served via a route in frontend_service.go.
# generated by the frontend build into public/ (not public/build).
COPY public/e2e-selectors.json public/e2e-selectors.json

ENTRYPOINT ["bin/grafana", "server"]
