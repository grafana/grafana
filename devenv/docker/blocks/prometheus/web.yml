basic_auth_users:
  # Generate a bcrypt hash for your chosen password and set it here before deploying.
  # Example (do NOT use default/weak passwords): htpasswd -nBC 12 "" | tr -d ':\n'
  # admin: <REPLACE_WITH_BCRYPT_HASH>
