<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match **>
  @type stdout
  format json
</match>

<match **>
  @type loki
  endpoint_url "http://loki:3100"
  labels {"env":"dev"}
  flush_interval 10s
  flush_at_shutdown true
  buffer_chunk_limit 1m
</match>