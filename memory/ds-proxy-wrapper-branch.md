---
name: ds-proxy-wrapper-branch
description: Architecture of the ds-proxy-wrapper branch decoupling the datasource HTTP proxy for apiserver integration
metadata:
  type: project
---

The `ds-proxy-wrapper` branch decouples the legacy datasource HTTP proxy (`pkg/api/pluginproxy`) from heavy deps so it can run inside the datasources apiserver.

Key pieces:

- `pluginproxy.DataSourceLoader` interface (loader.go) narrows the dependency from full `datasources.DataSourceService` to a small async-friendly surface returning the apiserver type `datasourcesV0.DataSource`. `loaderFromService` is the legacy-backed impl.
- `pluginproxy.HTTPContext` replaces `*contextmodel.ReqContext`; identity comes from `identity.GetRequester(ctx.Req.Context())`. Permissions use `requester.GetPermissions()` (the `GetPermissions` field was removed).
- URL validation moved from `pkg/api/datasource` to `pkg/api/datasource/validation` to break an import cycle.
- apiserver wiring: `datasource.ProvideProxyDependencies` (wire provider in `pkg/registry/apis/wireset.go`) builds a single `*ProxyDependencies` (ProxyCfg, validator, httpclient, oauth, tracer, features) instead of threading each into `RegisterAPIService`. `subProxyREST.Connect` (sub_proxy.go) builds a loader, validates, then runs `NewDataSourceProxy`.
- The proxy loader for the apiserver path is `datasourceLoader` (sub_proxy_loader.go): it adapts the existing `PluginDatasourceProvider` (via its public `GetDataSource` + `GetInstanceSettings`) into a `pluginproxy.DataSourceLoader`. Decrypted secrets come from `InstanceSettings.DecryptedSecureJSONData` (password/basicAuthPassword keys); transport from `InstanceSettings.HTTPClientOptions`. Deliberately NOT a method on `PluginDatasourceProvider` — keep that interface clean.
- `DataSourceRequestValidator` is a builder field (`DataSourceAPIBuilder.dataSourceRequestValidator`), NOT in `ProxyDependencies`, because it is shared: both the proxy and health subresources call `b.validateDataSourceRequest(url, jsonData, req)` to mirror the legacy HTTP API (legacy `/health` runs `DataSourceRequestValidator.Validate` before CheckHealth). sub_health.go runs CheckHealth in the handler (not Connect) so the request is available for validation. OSS validator is a no-op; real behavior is enterprise-only.
- Subresource path parsing (`proxyPathFromRequest`, `resourceRequest`) anchors on `/<name>/<subresource>` via `strings.Cut` (first occurrence), NOT `strings.LastIndex("/proxy"|"/resources")`, so a forwarded subpath that itself contains the subresource word isn't mis-split. Unit tests cover the embedded case.
- Integration tests: `pkg/tests/apis/datasource/testdata_test.go` `resources` subtest uses the testdata `/test/json` echo endpoint to assert method/path/body forwarding + 404. proxy_test.go covers the proxy (see above).

Integration coverage: `pkg/tests/apis/datasource/proxy_test.go` (`TestIntegrationDatasourceProxy`) creates a Prometheus datasource pointing at an httptest upstream and asserts both the legacy `/api/datasources/proxy/uid/<uid>/...` and apiserver `/apis/prometheus.datasource.grafana.app/v0alpha1/namespaces/default/datasources/<uid>/proxy/...` routes forward, plus cross-org denial. The OSS `DataSourceRequestValidator` is a no-op and the data-proxy whitelist is empty by default, so httptest (127.0.0.1) upstreams are allowed.

Enterprise: `pkg/extensions` is symlinked in locally (gitignored, not in OSS history). Changing OSS exported signatures (e.g. `NewDataSourceAPIBuilder`) breaks `pkg/extensions/apiserver/factory.go` — its two call sites pass `nil` for the new `proxyDeps` (proxy not wired in enterprise apiserver yet, like its nil decrypter). Build enterprise with `go build -tags "enterprise pro" ./pkg/server/ ./pkg/extensions/apiserver/`.

After changing `RegisterAPIService` signature, regenerate wire with: `go run ./pkg/build/wire/cmd/wire/main.go gen -tags "oss" -gen_tags "(!enterprise && !pro)" ./pkg/server` (plain `wire` fails — needs the oss build tag for `wireExtsSet`). `enterprise_wire_gen.go` is gitignored and regenerated by the enterprise tag variant.

Plan: make all changes here, then split into reviewable chunks (frontend/backend separate PRs per repo convention).
