{
  "uid": "fixture-existing-adhoc",
  "title": "Existing adhoc variable for the datasource is reused",
  "schemaVersion": 42,
  "templating": {
    "list": [
      {
        "name": "Filters",
        "type": "adhoc",
        "datasource": { "type": "prometheus", "uid": "prom-a" },
        "filters": [{ "key": "env", "operator": "=", "value": "prod" }],
        "baseFilters": []
      },
      {
        "name": "instance",
        "type": "query",
        "datasource": { "type": "prometheus", "uid": "prom-a" },
        "query": "label_values(up, instance)",
        "definition": "label_values(up, instance)",
        "current": { "text": "server-1", "value": "server-1" },
        "options": [],
        "refresh": 1,
        "regex": "",
        "sort": 0
      }
    ]
  },
  "panels": [
    {
      "id": 1,
      "type": "timeseries",
      "title": "Up",
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "datasource": { "type": "prometheus", "uid": "prom-a" },
      "targets": [
        {
          "refId": "A",
          "datasource": { "type": "prometheus", "uid": "prom-a" },
          "expr": "up{instance=~\"$instance\"}"
        }
      ]
    }
  ]
}
