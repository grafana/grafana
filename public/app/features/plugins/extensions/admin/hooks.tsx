import { useLocalStorage } from 'react-use';

export const usePersistExtension = (id: string, extension: any) => {};

export const useDeletePersistedExtension = (id: string) => {};

export const useGetPersistedExtension = (id: string) => {};

export const useGetPersistedExtensions = () => {};
