import React from 'react';

export const SortByHelper = () => {
  return (
    <div>
      <p>
        This transformation will sort each frame by the configured field. When &apos;reverse&apos; is checked, the
        values will return in the opposite order.
      </p>
    </div>
  );
};
