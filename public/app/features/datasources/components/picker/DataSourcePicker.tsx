import React from 'react';

import { DataSourceDropdown, DataSourceDropdownProps } from './DataSourceDropdown';

export function DataSourcePicker(props: DataSourceDropdownProps) {
  return <DataSourceDropdown {...props} />;
}
