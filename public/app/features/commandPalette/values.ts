export const RECENT_DASHBOARDS_PRORITY = 2;
export const DEFAULT_PRIORITY = 1;
export const SEARCH_RESULTS_PRORITY = 0; // Dynamic actions should be below static ones so the list doesn't 'jump' when they come in
