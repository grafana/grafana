import { CoauthorKeyboardPage } from './CoauthorKeyboardPage';

/**
 * V1 MVP of the highlight flow, in the same panel-editor chrome as the fuller
 * flows at /coauthor-keyboard and /coauthor-highlight. Reachable at
 * /coauthor-mvp.
 */
export function CoauthorMvpPage() {
  return <CoauthorKeyboardPage variant="mvp" />;
}

export default CoauthorMvpPage;
