/*
 * G6GraphWrapper Messages
 *
 * This contains all the text for the G6GraphWrapper component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'components.G6GraphWrapper';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the G6GraphWrapper component!',
  },
});
