// Code generated by copilot
import { Portal } from '@grafana/ui';
import { PreparedMarker, ResolvedMarker } from './markerTypes';

interface MarkerTooltipProps {
  position: { top: number; left: number };
}

export const MarkerTooltip = ({ position }: MarkerTooltipProps) => {
  const style = {
    top: position.top + 100,
    left: position.left + 100,
    position: 'absolute' as const,
    backgroundColor: 'rgba(252, 182, 182, 0.8)',
    color: 'white',
    padding: '5px',
    borderRadius: '3px',
    zIndex: 1000,
  };

  return (
    <Portal>
      <div style={style}>
        <div>
          <strong>{"a"}</strong>
        </div>
        <div>Value: {"31"}</div>
      </div>
    </Portal>
  );
};
// End of copilot-generated code
