import { type NavPersonaConfig } from './types';

export const platformGuy: NavPersonaConfig = {
  profileName: 'Centralized O11y Platform',
  orderedPins: ['connections', 'connections-datasources', 'connections-add-new-connection', 'plugins', 'cfg'],
};
