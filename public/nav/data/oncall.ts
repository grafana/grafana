import { type NavPersonaConfig } from './types';

export const oncallGuy: NavPersonaConfig = {
  profileName: 'On-Call',
  orderedPins: ['alerting', 'alert-rules', 'silences', 'groups', 'alerts-history'],
};
