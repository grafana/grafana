# 0.1.1 (2026-09-22)

- Add a `default` export condition to `./datasources` and `./apps` so CommonJS resolvers such as Jest can find the entrypoints. The package stays ESM-only; see the README for the Jest configuration this still needs.

# 0.1.0 (2026-09-22)

Initial release. `@grafana/plugin-compat` is in **ALPHA**; the API may change between minor versions.
