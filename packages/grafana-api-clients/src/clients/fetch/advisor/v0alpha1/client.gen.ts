/**
 * Generated by orval v7.17.0 🍺
 * Do not edit manually.
 * advisor.grafana.app/v0alpha1
 */
import type {
  APIResourceList,
  Check,
  CheckList,
  CheckType,
  CheckTypeList,
  CreateCheckParams,
  CreateCheckTypeParams,
  CreateRegisterResponse,
  DeleteCheckParams,
  DeleteCheckTypeParams,
  DeletecollectionCheckParams,
  DeletecollectionCheckTypeParams,
  GetCheckParams,
  GetCheckStatusParams,
  GetCheckTypeParams,
  GetCheckTypeStatusParams,
  ListCheckParams,
  ListCheckTypeParams,
  Patch,
  ReplaceCheckParams,
  ReplaceCheckStatusParams,
  ReplaceCheckTypeParams,
  ReplaceCheckTypeStatusParams,
  Status,
  UpdateCheckParams,
  UpdateCheckStatusParams,
  UpdateCheckTypeParams,
  UpdateCheckTypeStatusParams,
} from './client.schemas';

import { customFetch } from './baseApi';

/**
 * Describe the available kubernetes resources
 */
export type getAPIResourcesResponse200 = {
  data: APIResourceList;
  status: 200;
};

export type getAPIResourcesResponseSuccess = getAPIResourcesResponse200 & {
  headers: Headers;
};
export type getAPIResourcesResponse = getAPIResourcesResponseSuccess;

export const getGetAPIResourcesUrl = () => {
  return `/`;
};

export const getAPIResources = async (options?: RequestInit): Promise<getAPIResourcesResponse> => {
  return customFetch<getAPIResourcesResponse>(getGetAPIResourcesUrl(), {
    ...options,
    method: 'GET',
  });
};

/**
 * list or watch objects of kind Check
 */
export type listCheckResponse200 = {
  data: CheckList;
  status: 200;
};

export type listCheckResponseSuccess = listCheckResponse200 & {
  headers: Headers;
};
export type listCheckResponse = listCheckResponseSuccess;

export const getListCheckUrl = (params?: ListCheckParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/checks?${stringifiedParams}` : `/checks`;
};

export const listCheck = async (params?: ListCheckParams, options?: RequestInit): Promise<listCheckResponse> => {
  return customFetch<listCheckResponse>(getListCheckUrl(params), {
    ...options,
    method: 'GET',
  });
};

/**
 * create a Check
 */
export type createCheckResponse200 = {
  data: Check;
  status: 200;
};

export type createCheckResponse201 = {
  data: Check;
  status: 201;
};

export type createCheckResponse202 = {
  data: Check;
  status: 202;
};

export type createCheckResponseSuccess = (createCheckResponse200 | createCheckResponse201 | createCheckResponse202) & {
  headers: Headers;
};
export type createCheckResponse = createCheckResponseSuccess;

export const getCreateCheckUrl = (params?: CreateCheckParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/checks?${stringifiedParams}` : `/checks`;
};

export const createCheck = async (
  check: Check,
  params?: CreateCheckParams,
  options?: RequestInit
): Promise<createCheckResponse> => {
  return customFetch<createCheckResponse>(getCreateCheckUrl(params), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(check),
  });
};

/**
 * delete collection of Check
 */
export type deletecollectionCheckResponse200 = {
  data: Status;
  status: 200;
};

export type deletecollectionCheckResponseSuccess = deletecollectionCheckResponse200 & {
  headers: Headers;
};
export type deletecollectionCheckResponse = deletecollectionCheckResponseSuccess;

export const getDeletecollectionCheckUrl = (params?: DeletecollectionCheckParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/checks?${stringifiedParams}` : `/checks`;
};

export const deletecollectionCheck = async (
  params?: DeletecollectionCheckParams,
  options?: RequestInit
): Promise<deletecollectionCheckResponse> => {
  return customFetch<deletecollectionCheckResponse>(getDeletecollectionCheckUrl(params), {
    ...options,
    method: 'DELETE',
  });
};

/**
 * read the specified Check
 */
export type getCheckResponse200 = {
  data: Check;
  status: 200;
};

export type getCheckResponseSuccess = getCheckResponse200 & {
  headers: Headers;
};
export type getCheckResponse = getCheckResponseSuccess;

export const getGetCheckUrl = (name: string, params?: GetCheckParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {});

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/checks/${name}?${stringifiedParams}` : `/checks/${name}`;
};

export const getCheck = async (
  name: string,
  params?: GetCheckParams,
  options?: RequestInit
): Promise<getCheckResponse> => {
  return customFetch<getCheckResponse>(getGetCheckUrl(name, params), {
    ...options,
    method: 'GET',
  });
};

/**
 * replace the specified Check
 */
export type replaceCheckResponse200 = {
  data: Check;
  status: 200;
};

export type replaceCheckResponse201 = {
  data: Check;
  status: 201;
};

export type replaceCheckResponseSuccess = (replaceCheckResponse200 | replaceCheckResponse201) & {
  headers: Headers;
};
export type replaceCheckResponse = replaceCheckResponseSuccess;

export const getReplaceCheckUrl = (name: string, params?: ReplaceCheckParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/checks/${name}?${stringifiedParams}` : `/checks/${name}`;
};

export const replaceCheck = async (
  name: string,
  check: Check,
  params?: ReplaceCheckParams,
  options?: RequestInit
): Promise<replaceCheckResponse> => {
  return customFetch<replaceCheckResponse>(getReplaceCheckUrl(name, params), {
    ...options,
    method: 'PUT',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(check),
  });
};

/**
 * delete a Check
 */
export type deleteCheckResponse200 = {
  data: Status;
  status: 200;
};

export type deleteCheckResponse202 = {
  data: Status;
  status: 202;
};

export type deleteCheckResponseSuccess = (deleteCheckResponse200 | deleteCheckResponse202) & {
  headers: Headers;
};
export type deleteCheckResponse = deleteCheckResponseSuccess;

export const getDeleteCheckUrl = (name: string, params?: DeleteCheckParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/checks/${name}?${stringifiedParams}` : `/checks/${name}`;
};

export const deleteCheck = async (
  name: string,
  params?: DeleteCheckParams,
  options?: RequestInit
): Promise<deleteCheckResponse> => {
  return customFetch<deleteCheckResponse>(getDeleteCheckUrl(name, params), {
    ...options,
    method: 'DELETE',
  });
};

/**
 * partially update the specified Check
 */
export type updateCheckResponse200 = {
  data: Check;
  status: 200;
};

export type updateCheckResponse201 = {
  data: Check;
  status: 201;
};

export type updateCheckResponseSuccess = (updateCheckResponse200 | updateCheckResponse201) & {
  headers: Headers;
};
export type updateCheckResponse = updateCheckResponseSuccess;

export const getUpdateCheckUrl = (name: string, params?: UpdateCheckParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/checks/${name}?${stringifiedParams}` : `/checks/${name}`;
};

export const updateCheck = async (
  name: string,
  patch: Patch,
  params?: UpdateCheckParams,
  options?: RequestInit
): Promise<updateCheckResponse> => {
  return customFetch<updateCheckResponse>(getUpdateCheckUrl(name, params), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/apply-patch+yaml', ...options?.headers },
    body: JSON.stringify(patch),
  });
};

/**
 * read status of the specified Check
 */
export type getCheckStatusResponse200 = {
  data: Check;
  status: 200;
};

export type getCheckStatusResponseSuccess = getCheckStatusResponse200 & {
  headers: Headers;
};
export type getCheckStatusResponse = getCheckStatusResponseSuccess;

export const getGetCheckStatusUrl = (name: string, params?: GetCheckStatusParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {});

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/checks/${name}/status?${stringifiedParams}` : `/checks/${name}/status`;
};

export const getCheckStatus = async (
  name: string,
  params?: GetCheckStatusParams,
  options?: RequestInit
): Promise<getCheckStatusResponse> => {
  return customFetch<getCheckStatusResponse>(getGetCheckStatusUrl(name, params), {
    ...options,
    method: 'GET',
  });
};

/**
 * replace status of the specified Check
 */
export type replaceCheckStatusResponse200 = {
  data: Check;
  status: 200;
};

export type replaceCheckStatusResponse201 = {
  data: Check;
  status: 201;
};

export type replaceCheckStatusResponseSuccess = (replaceCheckStatusResponse200 | replaceCheckStatusResponse201) & {
  headers: Headers;
};
export type replaceCheckStatusResponse = replaceCheckStatusResponseSuccess;

export const getReplaceCheckStatusUrl = (name: string, params?: ReplaceCheckStatusParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/checks/${name}/status?${stringifiedParams}` : `/checks/${name}/status`;
};

export const replaceCheckStatus = async (
  name: string,
  check: Check,
  params?: ReplaceCheckStatusParams,
  options?: RequestInit
): Promise<replaceCheckStatusResponse> => {
  return customFetch<replaceCheckStatusResponse>(getReplaceCheckStatusUrl(name, params), {
    ...options,
    method: 'PUT',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(check),
  });
};

/**
 * partially update status of the specified Check
 */
export type updateCheckStatusResponse200 = {
  data: Check;
  status: 200;
};

export type updateCheckStatusResponse201 = {
  data: Check;
  status: 201;
};

export type updateCheckStatusResponseSuccess = (updateCheckStatusResponse200 | updateCheckStatusResponse201) & {
  headers: Headers;
};
export type updateCheckStatusResponse = updateCheckStatusResponseSuccess;

export const getUpdateCheckStatusUrl = (name: string, params?: UpdateCheckStatusParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/checks/${name}/status?${stringifiedParams}` : `/checks/${name}/status`;
};

export const updateCheckStatus = async (
  name: string,
  patch: Patch,
  params?: UpdateCheckStatusParams,
  options?: RequestInit
): Promise<updateCheckStatusResponse> => {
  return customFetch<updateCheckStatusResponse>(getUpdateCheckStatusUrl(name, params), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/apply-patch+yaml', ...options?.headers },
    body: JSON.stringify(patch),
  });
};

/**
 * list or watch objects of kind CheckType
 */
export type listCheckTypeResponse200 = {
  data: CheckTypeList;
  status: 200;
};

export type listCheckTypeResponseSuccess = listCheckTypeResponse200 & {
  headers: Headers;
};
export type listCheckTypeResponse = listCheckTypeResponseSuccess;

export const getListCheckTypeUrl = (params?: ListCheckTypeParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/checktypes?${stringifiedParams}` : `/checktypes`;
};

export const listCheckType = async (
  params?: ListCheckTypeParams,
  options?: RequestInit
): Promise<listCheckTypeResponse> => {
  return customFetch<listCheckTypeResponse>(getListCheckTypeUrl(params), {
    ...options,
    method: 'GET',
  });
};

/**
 * create a CheckType
 */
export type createCheckTypeResponse200 = {
  data: CheckType;
  status: 200;
};

export type createCheckTypeResponse201 = {
  data: CheckType;
  status: 201;
};

export type createCheckTypeResponse202 = {
  data: CheckType;
  status: 202;
};

export type createCheckTypeResponseSuccess = (
  | createCheckTypeResponse200
  | createCheckTypeResponse201
  | createCheckTypeResponse202
) & {
  headers: Headers;
};
export type createCheckTypeResponse = createCheckTypeResponseSuccess;

export const getCreateCheckTypeUrl = (params?: CreateCheckTypeParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/checktypes?${stringifiedParams}` : `/checktypes`;
};

export const createCheckType = async (
  checkType: CheckType,
  params?: CreateCheckTypeParams,
  options?: RequestInit
): Promise<createCheckTypeResponse> => {
  return customFetch<createCheckTypeResponse>(getCreateCheckTypeUrl(params), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(checkType),
  });
};

/**
 * delete collection of CheckType
 */
export type deletecollectionCheckTypeResponse200 = {
  data: Status;
  status: 200;
};

export type deletecollectionCheckTypeResponseSuccess = deletecollectionCheckTypeResponse200 & {
  headers: Headers;
};
export type deletecollectionCheckTypeResponse = deletecollectionCheckTypeResponseSuccess;

export const getDeletecollectionCheckTypeUrl = (params?: DeletecollectionCheckTypeParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/checktypes?${stringifiedParams}` : `/checktypes`;
};

export const deletecollectionCheckType = async (
  params?: DeletecollectionCheckTypeParams,
  options?: RequestInit
): Promise<deletecollectionCheckTypeResponse> => {
  return customFetch<deletecollectionCheckTypeResponse>(getDeletecollectionCheckTypeUrl(params), {
    ...options,
    method: 'DELETE',
  });
};

/**
 * read the specified CheckType
 */
export type getCheckTypeResponse200 = {
  data: CheckType;
  status: 200;
};

export type getCheckTypeResponseSuccess = getCheckTypeResponse200 & {
  headers: Headers;
};
export type getCheckTypeResponse = getCheckTypeResponseSuccess;

export const getGetCheckTypeUrl = (name: string, params?: GetCheckTypeParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {});

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/checktypes/${name}?${stringifiedParams}` : `/checktypes/${name}`;
};

export const getCheckType = async (
  name: string,
  params?: GetCheckTypeParams,
  options?: RequestInit
): Promise<getCheckTypeResponse> => {
  return customFetch<getCheckTypeResponse>(getGetCheckTypeUrl(name, params), {
    ...options,
    method: 'GET',
  });
};

/**
 * replace the specified CheckType
 */
export type replaceCheckTypeResponse200 = {
  data: CheckType;
  status: 200;
};

export type replaceCheckTypeResponse201 = {
  data: CheckType;
  status: 201;
};

export type replaceCheckTypeResponseSuccess = (replaceCheckTypeResponse200 | replaceCheckTypeResponse201) & {
  headers: Headers;
};
export type replaceCheckTypeResponse = replaceCheckTypeResponseSuccess;

export const getReplaceCheckTypeUrl = (name: string, params?: ReplaceCheckTypeParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/checktypes/${name}?${stringifiedParams}` : `/checktypes/${name}`;
};

export const replaceCheckType = async (
  name: string,
  checkType: CheckType,
  params?: ReplaceCheckTypeParams,
  options?: RequestInit
): Promise<replaceCheckTypeResponse> => {
  return customFetch<replaceCheckTypeResponse>(getReplaceCheckTypeUrl(name, params), {
    ...options,
    method: 'PUT',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(checkType),
  });
};

/**
 * delete a CheckType
 */
export type deleteCheckTypeResponse200 = {
  data: Status;
  status: 200;
};

export type deleteCheckTypeResponse202 = {
  data: Status;
  status: 202;
};

export type deleteCheckTypeResponseSuccess = (deleteCheckTypeResponse200 | deleteCheckTypeResponse202) & {
  headers: Headers;
};
export type deleteCheckTypeResponse = deleteCheckTypeResponseSuccess;

export const getDeleteCheckTypeUrl = (name: string, params?: DeleteCheckTypeParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/checktypes/${name}?${stringifiedParams}` : `/checktypes/${name}`;
};

export const deleteCheckType = async (
  name: string,
  params?: DeleteCheckTypeParams,
  options?: RequestInit
): Promise<deleteCheckTypeResponse> => {
  return customFetch<deleteCheckTypeResponse>(getDeleteCheckTypeUrl(name, params), {
    ...options,
    method: 'DELETE',
  });
};

/**
 * partially update the specified CheckType
 */
export type updateCheckTypeResponse200 = {
  data: CheckType;
  status: 200;
};

export type updateCheckTypeResponse201 = {
  data: CheckType;
  status: 201;
};

export type updateCheckTypeResponseSuccess = (updateCheckTypeResponse200 | updateCheckTypeResponse201) & {
  headers: Headers;
};
export type updateCheckTypeResponse = updateCheckTypeResponseSuccess;

export const getUpdateCheckTypeUrl = (name: string, params?: UpdateCheckTypeParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/checktypes/${name}?${stringifiedParams}` : `/checktypes/${name}`;
};

export const updateCheckType = async (
  name: string,
  patch: Patch,
  params?: UpdateCheckTypeParams,
  options?: RequestInit
): Promise<updateCheckTypeResponse> => {
  return customFetch<updateCheckTypeResponse>(getUpdateCheckTypeUrl(name, params), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/apply-patch+yaml', ...options?.headers },
    body: JSON.stringify(patch),
  });
};

/**
 * read status of the specified CheckType
 */
export type getCheckTypeStatusResponse200 = {
  data: CheckType;
  status: 200;
};

export type getCheckTypeStatusResponseSuccess = getCheckTypeStatusResponse200 & {
  headers: Headers;
};
export type getCheckTypeStatusResponse = getCheckTypeStatusResponseSuccess;

export const getGetCheckTypeStatusUrl = (name: string, params?: GetCheckTypeStatusParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {});

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/checktypes/${name}/status?${stringifiedParams}`
    : `/checktypes/${name}/status`;
};

export const getCheckTypeStatus = async (
  name: string,
  params?: GetCheckTypeStatusParams,
  options?: RequestInit
): Promise<getCheckTypeStatusResponse> => {
  return customFetch<getCheckTypeStatusResponse>(getGetCheckTypeStatusUrl(name, params), {
    ...options,
    method: 'GET',
  });
};

/**
 * replace status of the specified CheckType
 */
export type replaceCheckTypeStatusResponse200 = {
  data: CheckType;
  status: 200;
};

export type replaceCheckTypeStatusResponse201 = {
  data: CheckType;
  status: 201;
};

export type replaceCheckTypeStatusResponseSuccess = (
  | replaceCheckTypeStatusResponse200
  | replaceCheckTypeStatusResponse201
) & {
  headers: Headers;
};
export type replaceCheckTypeStatusResponse = replaceCheckTypeStatusResponseSuccess;

export const getReplaceCheckTypeStatusUrl = (name: string, params?: ReplaceCheckTypeStatusParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/checktypes/${name}/status?${stringifiedParams}`
    : `/checktypes/${name}/status`;
};

export const replaceCheckTypeStatus = async (
  name: string,
  checkType: CheckType,
  params?: ReplaceCheckTypeStatusParams,
  options?: RequestInit
): Promise<replaceCheckTypeStatusResponse> => {
  return customFetch<replaceCheckTypeStatusResponse>(getReplaceCheckTypeStatusUrl(name, params), {
    ...options,
    method: 'PUT',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(checkType),
  });
};

/**
 * partially update status of the specified CheckType
 */
export type updateCheckTypeStatusResponse200 = {
  data: CheckType;
  status: 200;
};

export type updateCheckTypeStatusResponse201 = {
  data: CheckType;
  status: 201;
};

export type updateCheckTypeStatusResponseSuccess = (
  | updateCheckTypeStatusResponse200
  | updateCheckTypeStatusResponse201
) & {
  headers: Headers;
};
export type updateCheckTypeStatusResponse = updateCheckTypeStatusResponseSuccess;

export const getUpdateCheckTypeStatusUrl = (name: string, params?: UpdateCheckTypeStatusParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0
    ? `/checktypes/${name}/status?${stringifiedParams}`
    : `/checktypes/${name}/status`;
};

export const updateCheckTypeStatus = async (
  name: string,
  patch: Patch,
  params?: UpdateCheckTypeStatusParams,
  options?: RequestInit
): Promise<updateCheckTypeStatusResponse> => {
  return customFetch<updateCheckTypeStatusResponse>(getUpdateCheckTypeStatusUrl(name, params), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/apply-patch+yaml', ...options?.headers },
    body: JSON.stringify(patch),
  });
};

export type createRegisterResponse200 = {
  data: CreateRegisterResponse;
  status: 200;
};

export type createRegisterResponseSuccess = createRegisterResponse200 & {
  headers: Headers;
};
export type createRegisterResponse = createRegisterResponseSuccess;

export const getCreateRegisterUrl = () => {
  return `/register`;
};

export const createRegister = async (options?: RequestInit): Promise<createRegisterResponse> => {
  return customFetch<createRegisterResponse>(getCreateRegisterUrl(), {
    ...options,
    method: 'POST',
  });
};
