/**
 * Generated by orval v7.17.0 🍺
 * Do not edit manually.
 * correlations.grafana.app/v0alpha1
 */
import type {
  APIResourceList,
  Correlation,
  CorrelationList,
  CreateCorrelationParams,
  DeleteCorrelationParams,
  DeletecollectionCorrelationParams,
  GetCorrelationParams,
  ListCorrelationParams,
  Patch,
  ReplaceCorrelationParams,
  Status,
  UpdateCorrelationParams,
} from './client.schemas';

import { customFetch } from './baseApi';

/**
 * Describe the available kubernetes resources
 */
export type getAPIResourcesResponse200 = {
  data: APIResourceList;
  status: 200;
};

export type getAPIResourcesResponseSuccess = getAPIResourcesResponse200 & {
  headers: Headers;
};
export type getAPIResourcesResponse = getAPIResourcesResponseSuccess;

export const getGetAPIResourcesUrl = () => {
  return `/`;
};

export const getAPIResources = async (options?: RequestInit): Promise<getAPIResourcesResponse> => {
  return customFetch<getAPIResourcesResponse>(getGetAPIResourcesUrl(), {
    ...options,
    method: 'GET',
  });
};

/**
 * list objects of kind Correlation
 */
export type listCorrelationResponse200 = {
  data: CorrelationList;
  status: 200;
};

export type listCorrelationResponseSuccess = listCorrelationResponse200 & {
  headers: Headers;
};
export type listCorrelationResponse = listCorrelationResponseSuccess;

export const getListCorrelationUrl = (params?: ListCorrelationParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/correlations?${stringifiedParams}` : `/correlations`;
};

export const listCorrelation = async (
  params?: ListCorrelationParams,
  options?: RequestInit
): Promise<listCorrelationResponse> => {
  return customFetch<listCorrelationResponse>(getListCorrelationUrl(params), {
    ...options,
    method: 'GET',
  });
};

/**
 * create a Correlation
 */
export type createCorrelationResponse200 = {
  data: Correlation;
  status: 200;
};

export type createCorrelationResponse201 = {
  data: Correlation;
  status: 201;
};

export type createCorrelationResponse202 = {
  data: Correlation;
  status: 202;
};

export type createCorrelationResponseSuccess = (
  | createCorrelationResponse200
  | createCorrelationResponse201
  | createCorrelationResponse202
) & {
  headers: Headers;
};
export type createCorrelationResponse = createCorrelationResponseSuccess;

export const getCreateCorrelationUrl = (params?: CreateCorrelationParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/correlations?${stringifiedParams}` : `/correlations`;
};

export const createCorrelation = async (
  correlation: Correlation,
  params?: CreateCorrelationParams,
  options?: RequestInit
): Promise<createCorrelationResponse> => {
  return customFetch<createCorrelationResponse>(getCreateCorrelationUrl(params), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(correlation),
  });
};

/**
 * delete collection of Correlation
 */
export type deletecollectionCorrelationResponse200 = {
  data: Status;
  status: 200;
};

export type deletecollectionCorrelationResponseSuccess = deletecollectionCorrelationResponse200 & {
  headers: Headers;
};
export type deletecollectionCorrelationResponse = deletecollectionCorrelationResponseSuccess;

export const getDeletecollectionCorrelationUrl = (params?: DeletecollectionCorrelationParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/correlations?${stringifiedParams}` : `/correlations`;
};

export const deletecollectionCorrelation = async (
  params?: DeletecollectionCorrelationParams,
  options?: RequestInit
): Promise<deletecollectionCorrelationResponse> => {
  return customFetch<deletecollectionCorrelationResponse>(getDeletecollectionCorrelationUrl(params), {
    ...options,
    method: 'DELETE',
  });
};

/**
 * read the specified Correlation
 */
export type getCorrelationResponse200 = {
  data: Correlation;
  status: 200;
};

export type getCorrelationResponseSuccess = getCorrelationResponse200 & {
  headers: Headers;
};
export type getCorrelationResponse = getCorrelationResponseSuccess;

export const getGetCorrelationUrl = (name: string, params?: GetCorrelationParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {});

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/correlations/${name}?${stringifiedParams}` : `/correlations/${name}`;
};

export const getCorrelation = async (
  name: string,
  params?: GetCorrelationParams,
  options?: RequestInit
): Promise<getCorrelationResponse> => {
  return customFetch<getCorrelationResponse>(getGetCorrelationUrl(name, params), {
    ...options,
    method: 'GET',
  });
};

/**
 * replace the specified Correlation
 */
export type replaceCorrelationResponse200 = {
  data: Correlation;
  status: 200;
};

export type replaceCorrelationResponse201 = {
  data: Correlation;
  status: 201;
};

export type replaceCorrelationResponseSuccess = (replaceCorrelationResponse200 | replaceCorrelationResponse201) & {
  headers: Headers;
};
export type replaceCorrelationResponse = replaceCorrelationResponseSuccess;

export const getReplaceCorrelationUrl = (name: string, params?: ReplaceCorrelationParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/correlations/${name}?${stringifiedParams}` : `/correlations/${name}`;
};

export const replaceCorrelation = async (
  name: string,
  correlation: Correlation,
  params?: ReplaceCorrelationParams,
  options?: RequestInit
): Promise<replaceCorrelationResponse> => {
  return customFetch<replaceCorrelationResponse>(getReplaceCorrelationUrl(name, params), {
    ...options,
    method: 'PUT',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(correlation),
  });
};

/**
 * delete a Correlation
 */
export type deleteCorrelationResponse200 = {
  data: Status;
  status: 200;
};

export type deleteCorrelationResponse202 = {
  data: Status;
  status: 202;
};

export type deleteCorrelationResponseSuccess = (deleteCorrelationResponse200 | deleteCorrelationResponse202) & {
  headers: Headers;
};
export type deleteCorrelationResponse = deleteCorrelationResponseSuccess;

export const getDeleteCorrelationUrl = (name: string, params?: DeleteCorrelationParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/correlations/${name}?${stringifiedParams}` : `/correlations/${name}`;
};

export const deleteCorrelation = async (
  name: string,
  params?: DeleteCorrelationParams,
  options?: RequestInit
): Promise<deleteCorrelationResponse> => {
  return customFetch<deleteCorrelationResponse>(getDeleteCorrelationUrl(name, params), {
    ...options,
    method: 'DELETE',
  });
};

/**
 * partially update the specified Correlation
 */
export type updateCorrelationResponse200 = {
  data: Correlation;
  status: 200;
};

export type updateCorrelationResponse201 = {
  data: Correlation;
  status: 201;
};

export type updateCorrelationResponseSuccess = (updateCorrelationResponse200 | updateCorrelationResponse201) & {
  headers: Headers;
};
export type updateCorrelationResponse = updateCorrelationResponseSuccess;

export const getUpdateCorrelationUrl = (name: string, params?: UpdateCorrelationParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/correlations/${name}?${stringifiedParams}` : `/correlations/${name}`;
};

export const updateCorrelation = async (
  name: string,
  patch: Patch,
  params?: UpdateCorrelationParams,
  options?: RequestInit
): Promise<updateCorrelationResponse> => {
  return customFetch<updateCorrelationResponse>(getUpdateCorrelationUrl(name, params), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/apply-patch+yaml', ...options?.headers },
    body: JSON.stringify(patch),
  });
};
