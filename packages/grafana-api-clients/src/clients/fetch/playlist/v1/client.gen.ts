/**
 * Generated by orval v7.17.0 🍺
 * Do not edit manually.
 * playlist.grafana.app/v1
 */
import type {
  APIResourceList,
  CreatePlaylistParams,
  DeletePlaylistParams,
  DeletecollectionPlaylistParams,
  GetPlaylistParams,
  GetPlaylistStatusParams,
  ListPlaylistParams,
  Patch,
  Playlist,
  PlaylistList,
  ReplacePlaylistParams,
  ReplacePlaylistStatusParams,
  Status,
  UpdatePlaylistParams,
  UpdatePlaylistStatusParams,
} from './client.schemas';

import { customFetch } from './baseApi';

/**
 * Describe the available kubernetes resources
 */
export type getAPIResourcesResponse200 = {
  data: APIResourceList;
  status: 200;
};

export type getAPIResourcesResponseSuccess = getAPIResourcesResponse200 & {
  headers: Headers;
};
export type getAPIResourcesResponse = getAPIResourcesResponseSuccess;

export const getGetAPIResourcesUrl = () => {
  return `/`;
};

export const getAPIResources = async (options?: RequestInit): Promise<getAPIResourcesResponse> => {
  return customFetch<getAPIResourcesResponse>(getGetAPIResourcesUrl(), {
    ...options,
    method: 'GET',
  });
};

/**
 * list or watch objects of kind Playlist
 */
export type listPlaylistResponse200 = {
  data: PlaylistList;
  status: 200;
};

export type listPlaylistResponseSuccess = listPlaylistResponse200 & {
  headers: Headers;
};
export type listPlaylistResponse = listPlaylistResponseSuccess;

export const getListPlaylistUrl = (params?: ListPlaylistParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/playlists?${stringifiedParams}` : `/playlists`;
};

export const listPlaylist = async (
  params?: ListPlaylistParams,
  options?: RequestInit
): Promise<listPlaylistResponse> => {
  return customFetch<listPlaylistResponse>(getListPlaylistUrl(params), {
    ...options,
    method: 'GET',
  });
};

/**
 * create a Playlist
 */
export type createPlaylistResponse200 = {
  data: Playlist;
  status: 200;
};

export type createPlaylistResponse201 = {
  data: Playlist;
  status: 201;
};

export type createPlaylistResponse202 = {
  data: Playlist;
  status: 202;
};

export type createPlaylistResponseSuccess = (
  | createPlaylistResponse200
  | createPlaylistResponse201
  | createPlaylistResponse202
) & {
  headers: Headers;
};
export type createPlaylistResponse = createPlaylistResponseSuccess;

export const getCreatePlaylistUrl = (params?: CreatePlaylistParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/playlists?${stringifiedParams}` : `/playlists`;
};

export const createPlaylist = async (
  playlist: Playlist,
  params?: CreatePlaylistParams,
  options?: RequestInit
): Promise<createPlaylistResponse> => {
  return customFetch<createPlaylistResponse>(getCreatePlaylistUrl(params), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(playlist),
  });
};

/**
 * delete collection of Playlist
 */
export type deletecollectionPlaylistResponse200 = {
  data: Status;
  status: 200;
};

export type deletecollectionPlaylistResponseSuccess = deletecollectionPlaylistResponse200 & {
  headers: Headers;
};
export type deletecollectionPlaylistResponse = deletecollectionPlaylistResponseSuccess;

export const getDeletecollectionPlaylistUrl = (params?: DeletecollectionPlaylistParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/playlists?${stringifiedParams}` : `/playlists`;
};

export const deletecollectionPlaylist = async (
  params?: DeletecollectionPlaylistParams,
  options?: RequestInit
): Promise<deletecollectionPlaylistResponse> => {
  return customFetch<deletecollectionPlaylistResponse>(getDeletecollectionPlaylistUrl(params), {
    ...options,
    method: 'DELETE',
  });
};

/**
 * read the specified Playlist
 */
export type getPlaylistResponse200 = {
  data: Playlist;
  status: 200;
};

export type getPlaylistResponseSuccess = getPlaylistResponse200 & {
  headers: Headers;
};
export type getPlaylistResponse = getPlaylistResponseSuccess;

export const getGetPlaylistUrl = (name: string, params?: GetPlaylistParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {});

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/playlists/${name}?${stringifiedParams}` : `/playlists/${name}`;
};

export const getPlaylist = async (
  name: string,
  params?: GetPlaylistParams,
  options?: RequestInit
): Promise<getPlaylistResponse> => {
  return customFetch<getPlaylistResponse>(getGetPlaylistUrl(name, params), {
    ...options,
    method: 'GET',
  });
};

/**
 * replace the specified Playlist
 */
export type replacePlaylistResponse200 = {
  data: Playlist;
  status: 200;
};

export type replacePlaylistResponse201 = {
  data: Playlist;
  status: 201;
};

export type replacePlaylistResponseSuccess = (replacePlaylistResponse200 | replacePlaylistResponse201) & {
  headers: Headers;
};
export type replacePlaylistResponse = replacePlaylistResponseSuccess;

export const getReplacePlaylistUrl = (name: string, params?: ReplacePlaylistParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/playlists/${name}?${stringifiedParams}` : `/playlists/${name}`;
};

export const replacePlaylist = async (
  name: string,
  playlist: Playlist,
  params?: ReplacePlaylistParams,
  options?: RequestInit
): Promise<replacePlaylistResponse> => {
  return customFetch<replacePlaylistResponse>(getReplacePlaylistUrl(name, params), {
    ...options,
    method: 'PUT',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(playlist),
  });
};

/**
 * delete a Playlist
 */
export type deletePlaylistResponse200 = {
  data: Status;
  status: 200;
};

export type deletePlaylistResponse202 = {
  data: Status;
  status: 202;
};

export type deletePlaylistResponseSuccess = (deletePlaylistResponse200 | deletePlaylistResponse202) & {
  headers: Headers;
};
export type deletePlaylistResponse = deletePlaylistResponseSuccess;

export const getDeletePlaylistUrl = (name: string, params?: DeletePlaylistParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/playlists/${name}?${stringifiedParams}` : `/playlists/${name}`;
};

export const deletePlaylist = async (
  name: string,
  params?: DeletePlaylistParams,
  options?: RequestInit
): Promise<deletePlaylistResponse> => {
  return customFetch<deletePlaylistResponse>(getDeletePlaylistUrl(name, params), {
    ...options,
    method: 'DELETE',
  });
};

/**
 * partially update the specified Playlist
 */
export type updatePlaylistResponse200 = {
  data: Playlist;
  status: 200;
};

export type updatePlaylistResponse201 = {
  data: Playlist;
  status: 201;
};

export type updatePlaylistResponseSuccess = (updatePlaylistResponse200 | updatePlaylistResponse201) & {
  headers: Headers;
};
export type updatePlaylistResponse = updatePlaylistResponseSuccess;

export const getUpdatePlaylistUrl = (name: string, params?: UpdatePlaylistParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/playlists/${name}?${stringifiedParams}` : `/playlists/${name}`;
};

export const updatePlaylist = async (
  name: string,
  patch: Patch,
  params?: UpdatePlaylistParams,
  options?: RequestInit
): Promise<updatePlaylistResponse> => {
  return customFetch<updatePlaylistResponse>(getUpdatePlaylistUrl(name, params), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/apply-patch+yaml', ...options?.headers },
    body: JSON.stringify(patch),
  });
};

/**
 * read status of the specified Playlist
 */
export type getPlaylistStatusResponse200 = {
  data: Playlist;
  status: 200;
};

export type getPlaylistStatusResponseSuccess = getPlaylistStatusResponse200 & {
  headers: Headers;
};
export type getPlaylistStatusResponse = getPlaylistStatusResponseSuccess;

export const getGetPlaylistStatusUrl = (name: string, params?: GetPlaylistStatusParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {});

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/playlists/${name}/status?${stringifiedParams}` : `/playlists/${name}/status`;
};

export const getPlaylistStatus = async (
  name: string,
  params?: GetPlaylistStatusParams,
  options?: RequestInit
): Promise<getPlaylistStatusResponse> => {
  return customFetch<getPlaylistStatusResponse>(getGetPlaylistStatusUrl(name, params), {
    ...options,
    method: 'GET',
  });
};

/**
 * replace status of the specified Playlist
 */
export type replacePlaylistStatusResponse200 = {
  data: Playlist;
  status: 200;
};

export type replacePlaylistStatusResponse201 = {
  data: Playlist;
  status: 201;
};

export type replacePlaylistStatusResponseSuccess = (
  | replacePlaylistStatusResponse200
  | replacePlaylistStatusResponse201
) & {
  headers: Headers;
};
export type replacePlaylistStatusResponse = replacePlaylistStatusResponseSuccess;

export const getReplacePlaylistStatusUrl = (name: string, params?: ReplacePlaylistStatusParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/playlists/${name}/status?${stringifiedParams}` : `/playlists/${name}/status`;
};

export const replacePlaylistStatus = async (
  name: string,
  playlist: Playlist,
  params?: ReplacePlaylistStatusParams,
  options?: RequestInit
): Promise<replacePlaylistStatusResponse> => {
  return customFetch<replacePlaylistStatusResponse>(getReplacePlaylistStatusUrl(name, params), {
    ...options,
    method: 'PUT',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(playlist),
  });
};

/**
 * partially update status of the specified Playlist
 */
export type updatePlaylistStatusResponse200 = {
  data: Playlist;
  status: 200;
};

export type updatePlaylistStatusResponse201 = {
  data: Playlist;
  status: 201;
};

export type updatePlaylistStatusResponseSuccess = (
  | updatePlaylistStatusResponse200
  | updatePlaylistStatusResponse201
) & {
  headers: Headers;
};
export type updatePlaylistStatusResponse = updatePlaylistStatusResponseSuccess;

export const getUpdatePlaylistStatusUrl = (name: string, params?: UpdatePlaylistStatusParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/playlists/${name}/status?${stringifiedParams}` : `/playlists/${name}/status`;
};

export const updatePlaylistStatus = async (
  name: string,
  patch: Patch,
  params?: UpdatePlaylistStatusParams,
  options?: RequestInit
): Promise<updatePlaylistStatusResponse> => {
  return customFetch<updatePlaylistStatusResponse>(getUpdatePlaylistStatusUrl(name, params), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/apply-patch+yaml', ...options?.headers },
    body: JSON.stringify(patch),
  });
};
