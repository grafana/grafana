/**
 * Generated by orval v7.17.0 🍺
 * Do not edit manually.
 * shorturl.grafana.app/v1beta1
 */
import type {
  APIResourceList,
  CreateShortURLParams,
  DeleteShortURLParams,
  DeletecollectionShortURLParams,
  GetGotoResponse,
  GetShortURLParams,
  GetShortURLStatusParams,
  ListShortURLParams,
  Patch,
  ReplaceShortURLParams,
  ReplaceShortURLStatusParams,
  ShortURL,
  ShortURLList,
  Status,
  UpdateShortURLParams,
  UpdateShortURLStatusParams,
} from './client.schemas';

import { customFetch } from './baseApi';

/**
 * Describe the available kubernetes resources
 */
export type getAPIResourcesResponse200 = {
  data: APIResourceList;
  status: 200;
};

export type getAPIResourcesResponseSuccess = getAPIResourcesResponse200 & {
  headers: Headers;
};
export type getAPIResourcesResponse = getAPIResourcesResponseSuccess;

export const getGetAPIResourcesUrl = () => {
  return `/`;
};

export const getAPIResources = async (options?: RequestInit): Promise<getAPIResourcesResponse> => {
  return customFetch<getAPIResourcesResponse>(getGetAPIResourcesUrl(), {
    ...options,
    method: 'GET',
  });
};

/**
 * list objects of kind ShortURL
 */
export type listShortURLResponse200 = {
  data: ShortURLList;
  status: 200;
};

export type listShortURLResponseSuccess = listShortURLResponse200 & {
  headers: Headers;
};
export type listShortURLResponse = listShortURLResponseSuccess;

export const getListShortURLUrl = (params?: ListShortURLParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/shorturls?${stringifiedParams}` : `/shorturls`;
};

export const listShortURL = async (
  params?: ListShortURLParams,
  options?: RequestInit
): Promise<listShortURLResponse> => {
  return customFetch<listShortURLResponse>(getListShortURLUrl(params), {
    ...options,
    method: 'GET',
  });
};

/**
 * create a ShortURL
 */
export type createShortURLResponse200 = {
  data: ShortURL;
  status: 200;
};

export type createShortURLResponse201 = {
  data: ShortURL;
  status: 201;
};

export type createShortURLResponse202 = {
  data: ShortURL;
  status: 202;
};

export type createShortURLResponseSuccess = (
  | createShortURLResponse200
  | createShortURLResponse201
  | createShortURLResponse202
) & {
  headers: Headers;
};
export type createShortURLResponse = createShortURLResponseSuccess;

export const getCreateShortURLUrl = (params?: CreateShortURLParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/shorturls?${stringifiedParams}` : `/shorturls`;
};

export const createShortURL = async (
  shortURL: ShortURL,
  params?: CreateShortURLParams,
  options?: RequestInit
): Promise<createShortURLResponse> => {
  return customFetch<createShortURLResponse>(getCreateShortURLUrl(params), {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(shortURL),
  });
};

/**
 * delete collection of ShortURL
 */
export type deletecollectionShortURLResponse200 = {
  data: Status;
  status: 200;
};

export type deletecollectionShortURLResponseSuccess = deletecollectionShortURLResponse200 & {
  headers: Headers;
};
export type deletecollectionShortURLResponse = deletecollectionShortURLResponseSuccess;

export const getDeletecollectionShortURLUrl = (params?: DeletecollectionShortURLParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/shorturls?${stringifiedParams}` : `/shorturls`;
};

export const deletecollectionShortURL = async (
  params?: DeletecollectionShortURLParams,
  options?: RequestInit
): Promise<deletecollectionShortURLResponse> => {
  return customFetch<deletecollectionShortURLResponse>(getDeletecollectionShortURLUrl(params), {
    ...options,
    method: 'DELETE',
  });
};

/**
 * read the specified ShortURL
 */
export type getShortURLResponse200 = {
  data: ShortURL;
  status: 200;
};

export type getShortURLResponseSuccess = getShortURLResponse200 & {
  headers: Headers;
};
export type getShortURLResponse = getShortURLResponseSuccess;

export const getGetShortURLUrl = (name: string, params?: GetShortURLParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {});

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/shorturls/${name}?${stringifiedParams}` : `/shorturls/${name}`;
};

export const getShortURL = async (
  name: string,
  params?: GetShortURLParams,
  options?: RequestInit
): Promise<getShortURLResponse> => {
  return customFetch<getShortURLResponse>(getGetShortURLUrl(name, params), {
    ...options,
    method: 'GET',
  });
};

/**
 * replace the specified ShortURL
 */
export type replaceShortURLResponse200 = {
  data: ShortURL;
  status: 200;
};

export type replaceShortURLResponse201 = {
  data: ShortURL;
  status: 201;
};

export type replaceShortURLResponseSuccess = (replaceShortURLResponse200 | replaceShortURLResponse201) & {
  headers: Headers;
};
export type replaceShortURLResponse = replaceShortURLResponseSuccess;

export const getReplaceShortURLUrl = (name: string, params?: ReplaceShortURLParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/shorturls/${name}?${stringifiedParams}` : `/shorturls/${name}`;
};

export const replaceShortURL = async (
  name: string,
  shortURL: ShortURL,
  params?: ReplaceShortURLParams,
  options?: RequestInit
): Promise<replaceShortURLResponse> => {
  return customFetch<replaceShortURLResponse>(getReplaceShortURLUrl(name, params), {
    ...options,
    method: 'PUT',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(shortURL),
  });
};

/**
 * delete a ShortURL
 */
export type deleteShortURLResponse200 = {
  data: Status;
  status: 200;
};

export type deleteShortURLResponse202 = {
  data: Status;
  status: 202;
};

export type deleteShortURLResponseSuccess = (deleteShortURLResponse200 | deleteShortURLResponse202) & {
  headers: Headers;
};
export type deleteShortURLResponse = deleteShortURLResponseSuccess;

export const getDeleteShortURLUrl = (name: string, params?: DeleteShortURLParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/shorturls/${name}?${stringifiedParams}` : `/shorturls/${name}`;
};

export const deleteShortURL = async (
  name: string,
  params?: DeleteShortURLParams,
  options?: RequestInit
): Promise<deleteShortURLResponse> => {
  return customFetch<deleteShortURLResponse>(getDeleteShortURLUrl(name, params), {
    ...options,
    method: 'DELETE',
  });
};

/**
 * partially update the specified ShortURL
 */
export type updateShortURLResponse200 = {
  data: ShortURL;
  status: 200;
};

export type updateShortURLResponse201 = {
  data: ShortURL;
  status: 201;
};

export type updateShortURLResponseSuccess = (updateShortURLResponse200 | updateShortURLResponse201) & {
  headers: Headers;
};
export type updateShortURLResponse = updateShortURLResponseSuccess;

export const getUpdateShortURLUrl = (name: string, params?: UpdateShortURLParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/shorturls/${name}?${stringifiedParams}` : `/shorturls/${name}`;
};

export const updateShortURL = async (
  name: string,
  patch: Patch,
  params?: UpdateShortURLParams,
  options?: RequestInit
): Promise<updateShortURLResponse> => {
  return customFetch<updateShortURLResponse>(getUpdateShortURLUrl(name, params), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/apply-patch+yaml', ...options?.headers },
    body: JSON.stringify(patch),
  });
};

/**
 * connect GET requests to goto of ShortURL
 */
export type getShortURLGotoResponse200 = {
  data: GetGotoResponse;
  status: 200;
};

export type getShortURLGotoResponseSuccess = getShortURLGotoResponse200 & {
  headers: Headers;
};
export type getShortURLGotoResponse = getShortURLGotoResponseSuccess;

export const getGetShortURLGotoUrl = (name: string) => {
  return `/shorturls/${name}/goto`;
};

export const getShortURLGoto = async (name: string, options?: RequestInit): Promise<getShortURLGotoResponse> => {
  return customFetch<getShortURLGotoResponse>(getGetShortURLGotoUrl(name), {
    ...options,
    method: 'GET',
  });
};

/**
 * read status of the specified ShortURL
 */
export type getShortURLStatusResponse200 = {
  data: ShortURL;
  status: 200;
};

export type getShortURLStatusResponseSuccess = getShortURLStatusResponse200 & {
  headers: Headers;
};
export type getShortURLStatusResponse = getShortURLStatusResponseSuccess;

export const getGetShortURLStatusUrl = (name: string, params?: GetShortURLStatusParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {});

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/shorturls/${name}/status?${stringifiedParams}` : `/shorturls/${name}/status`;
};

export const getShortURLStatus = async (
  name: string,
  params?: GetShortURLStatusParams,
  options?: RequestInit
): Promise<getShortURLStatusResponse> => {
  return customFetch<getShortURLStatusResponse>(getGetShortURLStatusUrl(name, params), {
    ...options,
    method: 'GET',
  });
};

/**
 * replace status of the specified ShortURL
 */
export type replaceShortURLStatusResponse200 = {
  data: ShortURL;
  status: 200;
};

export type replaceShortURLStatusResponse201 = {
  data: ShortURL;
  status: 201;
};

export type replaceShortURLStatusResponseSuccess = (
  | replaceShortURLStatusResponse200
  | replaceShortURLStatusResponse201
) & {
  headers: Headers;
};
export type replaceShortURLStatusResponse = replaceShortURLStatusResponseSuccess;

export const getReplaceShortURLStatusUrl = (name: string, params?: ReplaceShortURLStatusParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/shorturls/${name}/status?${stringifiedParams}` : `/shorturls/${name}/status`;
};

export const replaceShortURLStatus = async (
  name: string,
  shortURL: ShortURL,
  params?: ReplaceShortURLStatusParams,
  options?: RequestInit
): Promise<replaceShortURLStatusResponse> => {
  return customFetch<replaceShortURLStatusResponse>(getReplaceShortURLStatusUrl(name, params), {
    ...options,
    method: 'PUT',
    headers: { 'Content-Type': 'application/json', ...options?.headers },
    body: JSON.stringify(shortURL),
  });
};

/**
 * partially update status of the specified ShortURL
 */
export type updateShortURLStatusResponse200 = {
  data: ShortURL;
  status: 200;
};

export type updateShortURLStatusResponse201 = {
  data: ShortURL;
  status: 201;
};

export type updateShortURLStatusResponseSuccess = (
  | updateShortURLStatusResponse200
  | updateShortURLStatusResponse201
) & {
  headers: Headers;
};
export type updateShortURLStatusResponse = updateShortURLStatusResponseSuccess;

export const getUpdateShortURLStatusUrl = (name: string, params?: UpdateShortURLStatusParams) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {
    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : value.toString());
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/shorturls/${name}/status?${stringifiedParams}` : `/shorturls/${name}/status`;
};

export const updateShortURLStatus = async (
  name: string,
  patch: Patch,
  params?: UpdateShortURLStatusParams,
  options?: RequestInit
): Promise<updateShortURLStatusResponse> => {
  return customFetch<updateShortURLStatusResponse>(getUpdateShortURLStatusUrl(name, params), {
    ...options,
    method: 'PATCH',
    headers: { 'Content-Type': 'application/apply-patch+yaml', ...options?.headers },
    body: JSON.stringify(patch),
  });
};
