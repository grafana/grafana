import { ArgTypes } from '@storybook/blocks';
import { StateDot } from './StateDot';

# StateDot

A component for showing the state and health of a rule without the text component.

> ⚠️ Avoid using this component by itself as it's not accessible to users with various forms of colorblindness.
> Consider using the `<StateText />` or `<StateIcon />` components instead.
