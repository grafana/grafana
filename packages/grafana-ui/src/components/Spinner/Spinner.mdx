import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Spinner } from './Spinner';

<Meta title="MDX|Spinner" component={Spinner} />

# Spinner

Spinner is `fa-spinner` icon animated. It should be used when some information is being loaded. When the loading is completed, the spinner must disappear. 

<Preview>
	<div>
		<Spinner />
		<Spinner size={24}/>
	</div>
</Preview>

<Props of={Spinner} />