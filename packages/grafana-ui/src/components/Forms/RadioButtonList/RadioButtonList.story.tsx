import { Meta, Story } from '@storybook/react';
import React from 'react';

import { RadioButtonList } from './RadioButtonList';

export default {
  title: 'Forms/RadioButtonList',
  component: RadioButtonList,
} as Meta;

export const Default = () => <RadioButtonList />;
