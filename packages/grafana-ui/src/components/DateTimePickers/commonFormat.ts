// The moment.js format used for datetime inputs in the TimeRangePicker so the
// parsed/printed value stays consistent regardless of the configured
// systemDateFormats.fullDate.
export const commonFormat = 'YYYY-MM-DD HH:mm:ss';
