import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Indicator from './Indicator';

<Meta title="MDX|Indicator" component={Indicator} />

# Indicator

Used to indicate status inline/near fields, such as a <Indicator>Saved</Indicator> indicator next to fields, or a temporary <Indicator suffixIcon="check">Copied</Indicator> indicator above a button

<Props of={Indicator} />
