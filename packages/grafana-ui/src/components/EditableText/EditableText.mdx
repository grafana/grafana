import { Props, ArgsTable } from '@storybook/addon-docs/blocks';
import { EditableText } from './EditableText';

# EditableText

Use for showing text which can be edited inline.

<ArgsTable of={EditableText} />
