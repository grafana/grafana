import { ArgTypes } from '@storybook/blocks';
import { Table } from './Table';

# DataGrid

A wrapper around [react-data-grid](https://github.com/adazzle/react-data-grid) which powers the Table visualization in Grafana (as of Grafana 12.2).
