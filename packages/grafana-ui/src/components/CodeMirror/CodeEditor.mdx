import { ArgTypes, Meta } from '@storybook/addon-docs/blocks';
import { CodeEditor } from './CodeEditor';

<Meta title="MDX|CodeMirrorEditor" component={CodeEditor} />

# CodeMirrorEditor

A code editor component using CodeMirror.

### Usage

```tsx
import { CodeMirrorEditor } from '@grafana/ui/unstable';

<CodeMirrorEditor value="SELECT 1" onChange={() => {}} />;
```

## Props

<ArgTypes of={CodeEditor} />
