// The legacy react-table implementation has been removed; the public `Table` is now the
// (currently unstable) TableNG. Props differ from the old table — see release notes.
export { TableNG as Table } from './TableNG/TableNG';
