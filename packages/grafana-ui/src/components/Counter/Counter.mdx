import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Counter } from './Counter';

<Meta title="MDX|Counter" component={Counter} />

# Counter

A composition component for rendering a counter label

### Usage

```jsx
import { Counter } from '@grafana/ui';

<Counter value={123456789} />
```

### Props
<Props of={Counter} />

