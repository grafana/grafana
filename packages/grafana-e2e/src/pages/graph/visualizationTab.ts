import { pageFactory } from '../../support';
import { Selectors } from '../../selectors';

export const VisualizationTab = pageFactory({
  url: '',
  selectors: Selectors.Panels.Visualization.Graph.VisualizationTab,
});
