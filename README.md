# Asset branch

Screenshots referenced from pull request descriptions. GitHub has no API for
attaching images to a PR body, so they are committed here and linked with
commit-sha-pinned raw URLs.

This branch holds no code and is never merged. One directory per topic under
`.screenshots/`.
