name: Manage Go Cache
description: >-
  Logs GOMODCACHE and GOCACHE sizes at setup time.
  As a post step, trims the Go test cache (go clean -testcache)
  before the cache-save step runs, then reports final sizes.

runs:
  using: node20
  main: index.js
  post: post.js
