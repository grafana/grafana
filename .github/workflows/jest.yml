name: Frontend Unit Tests

on:
  pull_request:
    paths:
    - '**.ts'
    - '**.tsx'

jobs:
  build:
    name: Run ESLint
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Run Jest
      uses: stefanoeb/jest-action@1.0.2
      with:
        jestArgs: --ci --maxWorkers 2
