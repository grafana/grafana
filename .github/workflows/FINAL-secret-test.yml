name: FINAL - Secret Exposure Test
on: [workflow_dispatch]

env:
  ACTIONS_STEP_DEBUG: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test 1 - Simulated GitHub Secret
        env:
          SIMULATED_VAULT_SECRET: ${{ 'azure-real-password-123' }}
        run: |
          echo "Testing secret exposure..."
          # Command that might leak
          env | grep -i secret
          
      - name: Test 2 - Direct echo
        env:
          ANOTHER_SECRET: direct-env-var-456
        run: |
          echo "Secret value: $ANOTHER_SECRET"
