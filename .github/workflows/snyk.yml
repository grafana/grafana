name: Snyk SBOM CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  snyk_scans:
    
    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v3
      - name: Run Snyk to check for vulnerabilities - sarif output
        continue-on-error: true
        uses: snyk/actions/node@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          command: test
          args:  --all-projects --sarif-file-output=snyk.sarif --strict-out-of-sync=false

      - name: Run Snyk to check for vulnerabilities - json output
        continue-on-error: true
        uses: snyk/actions/node@master
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        with:
          command: test
          args: --json --all-projects --json-file-output=snyk.json  --strict-out-of-sync=false

      - name: Use Node.js 18.x
        uses: actions/setup-node@v2
        with:
          node-version: 18.x
          cache: 'npm'

      - name: install snyk-to-html
        run: |
          npm install snyk-to-html -g
          snyk-to-html -i snyk.json -o snyk.html

      - name: Upload result to GitHub Code Scanning
        uses: github/codeql-action/upload-sarif@v2
        with:
          sarif_file: snyk.sarif

      - name: Create results dir
        run: mkdir -p snyk_scans && cp -v snyk.sarif snyk.json snyk.html snyk_scans/

      - name: Use the Upload Artifact GitHub Action
        uses: actions/upload-artifact@v2
        with:
          name: snyk_scans
          path: snyk_scans

