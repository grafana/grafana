name: PR Checks
on:
  pull_request:
    types: [opened, synchronize, reopened, labeled, unlabeled]

concurrency:
  group: pr-checks-${{ github.event.number }}-new
  cancel-in-progress: true

permissions: {}

jobs:
  main:
    runs-on: ubuntu-arm64-small
    permissions:
      statuses: write
      checks: write
      actions: write
      contents: read
      pull-requests: read
    if: github.event.pull_request.draft == false
    steps:
      - name: Run PR Checks
        uses: grafana/grafana-github-actions/pr-checks@3c62d35c5a66e730186a338e45aeb8fa784e2d56 # main
        with:
          token: ${{secrets.GITHUB_TOKEN}}
          configPath: pr-checks
