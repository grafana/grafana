name: CONTAINER SECRET EXPOSURE PROOF
on: [workflow_dispatch]

jobs:
  prove-container-exposure:
    runs-on: ubuntu-latest
    steps:
      - run: |
          docker run --name test-container -e SECRET_PASSWORD=azure-real-password-12345 alpine env > /tmp/container-env.txt
          echo "Container environment dump:"
          cat /tmp/container-env.txt | grep SECRET_PASSWORD
          docker rm test-container
