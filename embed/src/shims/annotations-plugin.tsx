/**
 * The real AnnotationsPlugin pulls app annotation and dashboard services (and from
 * there api-clients and an alerting worker). Annotations are dashboard-app context.
 */
export function AnnotationsPlugin(_props: Record<string, unknown>) {
  return null;
}
