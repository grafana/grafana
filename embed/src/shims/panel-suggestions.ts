/**
 * Panel-type suggestions are an in-app editing feature (suggest a viz switch when the
 * data does not fit); the chain reaches app/api -> @grafana/api-clients.
 */
export const getPrepareTimeseriesSuggestion = () => undefined;
