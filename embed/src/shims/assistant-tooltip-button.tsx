/**
 * The real button imports @grafana/assistant, which reaches api-clients and the
 * alerting web worker. An embedded panel has no Grafana Assistant to open.
 */
export function AssistantTooltipButton(_props: Record<string, unknown>) {
  return null;
}
