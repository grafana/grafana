# Panel test

Apply the **`panel-testing-strategy`** skill to the target below. Do not restate the whole skill — read and follow it.

## Target

Use `$ARGUMENTS` if provided; otherwise the currently open / focused file. If neither is available, ask which panel or file to cover.

## Outcome

Add or extend co-located unit tests that assert concrete behavior (not “it rendered”), matching neighboring test style, and report what you ran to verify.
