<!-- we define these as global variables so we can refer to it when we are ion a different scope, ie. looping over alerts -->
<mj-raw position="file-start">
{{- $theme := default "dark" .Theme }}
{{- $wantsDarkTheme := eq $theme "dark" }}
</mj-raw>
