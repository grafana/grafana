package resources

import (
	"github.com/google/wire"
	{{range .Kinds }}
	"{{ $.KindPackagePrefix }}/{{ .Props.MachineName }}"{{end}}
)

var GeneratedWireSet = wire.NewSet({{range $i, $k := .Kinds }}
	{{ $k.Props.MachineName }}.GeneratedWireSet,{{end}}
)