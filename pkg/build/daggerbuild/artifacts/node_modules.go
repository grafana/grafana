package artifacts

import (
	"context"
	"log/slog"
	"path/filepath"

	"dagger.io/dagger"
	"github.com/grafana/grafana/pkg/build/daggerbuild/arguments"
	"github.com/grafana/grafana/pkg/build/daggerbuild/flags"
	"github.com/grafana/grafana/pkg/build/daggerbuild/frontend"
	"github.com/grafana/grafana/pkg/build/daggerbuild/pipeline"
)

var (
	NodeModulesFlags     = flags.PackageNameFlags
	NodeModulesArguments = []pipeline.Argument{
		arguments.YarnCacheDirectory,
	}
)

var NodeModulesInitializer = Initializer{
	InitializerFunc: NewNodeModulesFromString,
	Arguments:       NodeModulesArguments,
}

type NodeModules struct {
	Src       *dagger.Directory
	YarnCache *dagger.CacheVolume
	Version   string
}

// The frontend does not have any artifact dependencies.
func (f *NodeModules) Dependencies(ctx context.Context) ([]*pipeline.Artifact, error) {
	return nil, nil
}

// Builder will return a node.js alpine container that matches the .nvmrc in the Grafana source repository
func (f *NodeModules) Builder(ctx context.Context, opts *pipeline.ArtifactContainerOpts) (*dagger.Container, error) {
	return FrontendBuilder(ctx, f.Src, f.YarnCache, opts)
}

func (f *NodeModules) BuildFile(ctx context.Context, builder *dagger.Container, opts *pipeline.ArtifactContainerOpts) (*dagger.File, error) {
	panic("not implemented") // NodeModules doesn't return a file
}

func (f *NodeModules) BuildDir(ctx context.Context, builder *dagger.Container, opts *pipeline.ArtifactContainerOpts) (*dagger.Directory, error) {
	return frontend.WithYarnCache(frontend.WithFrontendSource(builder, f.Src), f.YarnCache).
		WithExec([]string{"yarn", "install", "--immutable"}).
		Directory("node_modules"), nil
}

func (f *NodeModules) Publisher(ctx context.Context, opts *pipeline.ArtifactContainerOpts) (*dagger.Container, error) {
	panic("not implemented") // TODO: Implement
}

func (f *NodeModules) PublishFile(ctx context.Context, opts *pipeline.ArtifactPublishFileOpts) error {
	panic("not implemented") // TODO: Implement
}

func (f *NodeModules) PublishDir(ctx context.Context, opts *pipeline.ArtifactPublishDirOpts) error {
	panic("not implemented") // TODO: Implement
}

func (f *NodeModules) VerifyFile(ctx context.Context, client *dagger.Client, file *dagger.File) error {
	// Not a file
	return nil
}

func (f *NodeModules) VerifyDirectory(ctx context.Context, client *dagger.Client, dir *dagger.Directory) error {
	// Nothing to verify (yet?)
	return nil
}

func (f *NodeModules) String() string {
	return "node-modules"
}

// Filename should return a deterministic file or folder name that this build will produce.
// This filename is used as a map key for caching, so implementers need to ensure that arguments or flags that affect the output
// also affect the filename to ensure that there are no collisions.
// For example, the backend for `linux/amd64` and `linux/arm64` should not both produce a `bin` folder, they should produce a
// `bin/linux-amd64` folder and a `bin/linux-arm64` folder. Callers can mount this as `bin` or whatever if they want.
func (f *NodeModules) Filename(ctx context.Context) (string, error) {
	// Important note: this path is only used in two ways:
	// 1. When requesting an artifact be built and exported, this is the path where it will be exported to
	// 2. In a map to distinguish when the same artifact is being built more than once
	return filepath.Join(f.Version, "node-modules"), nil
}

func NewNodeModulesFromString(ctx context.Context, log *slog.Logger, artifact string, state pipeline.StateHandler) (*pipeline.Artifact, error) {
	grafanaDir, err := GrafanaDir(ctx, state, false)
	if err != nil {
		return nil, err
	}
	cache, err := state.CacheVolume(ctx, arguments.YarnCacheDirectory)
	if err != nil {
		return nil, err
	}
	version, err := state.String(ctx, arguments.Version)
	if err != nil {
		return nil, err
	}

	return NewNodeModules(ctx, log, artifact, grafanaDir, version, cache)
}

func NewNodeModules(ctx context.Context, log *slog.Logger, artifact string, src *dagger.Directory, version string, cache *dagger.CacheVolume) (*pipeline.Artifact, error) {
	return pipeline.ArtifactWithLogging(ctx, log, &pipeline.Artifact{
		ArtifactString: artifact,
		Type:           pipeline.ArtifactTypeDirectory,
		Flags:          NodeModulesFlags,
		Handler: &NodeModules{
			Src:       src,
			YarnCache: cache,
			Version:   version,
		},
	})
}
