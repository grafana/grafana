package commands

// migrateDatabaseCommand is just a stub that does nothing. Migrations are run as part of every database command
// and so we just need that and this can be empty.
func migrateDatabaseCommand(c CommandLine) error {
	return nil
}
